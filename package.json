{
  "name": "dreamland-mud-client",
  "description": "Dreamland MUD Client",
  "version": "0.0.1",
  "devDependencies": {
    "browserify": "^16.2.3",
    "uglify-js": "^3.5.11"
  },
  "dependencies": {
    "brace": "^0.11.1"
  },
  "scripts": {
    "build": "browserify src/main.js --debug | uglifyjs -cm -o dist/bundle.js --source-map content=inline,filename=dist/bundle.js,url=dist/bundle.js.map"
  }
}
